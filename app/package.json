{
  "name": "wordpress-frontend-proxy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "bun run index.js",
    "format": "prettier --write '**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["WordPress", "proxy"],
  "author": "Pat Heard",
  "license": "MIT",
  "description": "Serves as a proxy for the WordPress API.",
  "dependencies": {
    "express": "^4.21.2",
    "express-handlebars": "^8.0.1"
  },
  "devDependencies": {
    "prettier": "^3.5.1"
  }
}
