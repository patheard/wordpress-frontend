{
  "name": "wordpress-frontend-proxy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "bun run index.js",
    "format": "prettier --write '**/*.ts' '**/*.js'",
    "perf": "k6 run tests/performance.js -e URL=https://your-url.ca --iterations 100"
  },
  "keywords": [
    "WordPress",
    "proxy"
  ],
  "author": "Pat Heard",
  "license": "MIT",
  "description": "Serves as a proxy for the WordPress API.",
  "dependencies": {
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "prettier": "^3.5.1"
  }
}
